{
  "baseline_version": 1,
  "request_schema_id": "urn:enterprise-ai-platform:provider:invocation-request:v1",
  "result_schema_id": "urn:enterprise-ai-platform:provider:invocation-result:v1",
  "runtime_config_schema_id": "urn:enterprise-ai-platform:provider:runtime-config:v1",
  "registry_schema_id": "urn:enterprise-ai-platform:provider:adapter-registry:v1",
  "required_request_fields": ["schema_version", "request_id", "trace_id", "tenant_id", "config_version", "route", "operation", "model_alias", "messages", "stream", "obligations"],
  "required_result_fields": ["schema_version", "request_id", "trace_id", "tenant_id", "config_version", "model_alias", "provider_id", "adapter_id", "adapter_version", "outcome", "reason_code", "error_kind", "retry_hint", "response_mode", "response"],
  "required_runtime_config_fields": ["schema_version", "tenant_id", "config_version", "provider_id", "adapter_id", "provider_model", "endpoint", "secret_ref"],
  "required_registry_fields": ["schema_version", "tenant_id", "config_version", "adapters"],
  "required_outcomes": ["succeeded", "failed", "indeterminate"],
  "required_runtime_kinds": ["native", "litellm"]
}
