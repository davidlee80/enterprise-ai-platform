# PostgreSQL module contract

Captures a reviewed engine-version input, capacity-profile reference, high
availability, backup retention, encryption reference, and separated logical
database requirements. Credentials, endpoints, instance classes, and provider
resources are intentionally absent until their provider and sizing decisions
are reviewed.

