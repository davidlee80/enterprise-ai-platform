# DNS module contract

Defines zone purposes and visibility without selecting a domain, registrar,
certificate issuer, or cloud DNS product. Those choices remain
`TBD-011`/`TBD-019` and must be supplied through reviewed environment inputs.

