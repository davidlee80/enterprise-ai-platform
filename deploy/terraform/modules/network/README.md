# Network module contract

Validates an approved address space and private subnet set. Routing, NAT,
firewalls, public ingress, peering, and provider resource identifiers remain
provider adapter responsibilities after `TBD-011`. No production CIDR is
selected here.

