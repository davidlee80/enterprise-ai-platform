# Object Storage module contract

Defines domain ownership, versioning, public-access blocking, and encryption
references. It explicitly forbids Gateway-owned direct RAG storage access;
knowledge-domain access uses a stable versioned interface. Bucket names,
lifecycle/retention, replication, and provider resources remain reviewed inputs.

