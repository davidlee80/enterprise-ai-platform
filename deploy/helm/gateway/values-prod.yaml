environment: production

replicaCount: 3

image:
  tag: ""
  digest: ""

podDisruptionBudget:
  enabled: true
  minAvailable: 2

topologySpreadConstraints:
  - maxSkew: 1
    topologyKey: kubernetes.io/hostname
    whenUnsatisfiable: DoNotSchedule

# TBD-019: supplied only by reviewed production environment configuration.
production:
  domain: ""
  namespace: ""
  certificateIssuer: ""
  storageClass: ""
