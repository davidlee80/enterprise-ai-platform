<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <title><%= manifest.content.title %></title>
    <link rel="stylesheet" href="./style.css" />
  </head>

  <body>
    <div class="poster" id="poster">
      <header
        class="hero"
        style="background-image:
          linear-gradient(
            to bottom,
            rgba(255,255,255,.05),
            rgba(248,251,240,.55)
          ),
          url('<%= manifest.assets.hero.resolvedUrl %>')"
      >
        <h1 id="posterTitle"></h1>
        <div id="posterSubtitle" class="subtitle"></div>
      </header>

      <main class="content">
        <section class="route-card">
          <div class="section-title">核心路线示意图</div>
          <div id="routeMap" class="route-map"></div>
        </section>

        <section class="schedule-card">
          <div class="section-title">5天路线速览</div>
          <div id="schedule"></div>
        </section>

        <section class="tips-card">
          <div class="section-title light">备注事项 / 出行建议</div>
          <div id="tips"></div>
        </section>

        <section class="budget-card">
          <div class="section-title light">花销明细（参考）</div>
          <div id="budgetItems"></div>
          <div id="budgetTotal"></div>
        </section>
      </main>
    </div>

    <script>
      window.__POSTER_MANIFEST__ =
        <%- safeJson(manifest) %>;
    </script>

    <script src="./render.js"></script>
  </body>
</html>
